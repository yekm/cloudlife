function(add_cloudlife_test name)
    add_executable(${name} ${ARGN})
    target_include_directories(${name} PRIVATE $<TARGET_PROPERTY:clouds,INCLUDE_DIRECTORIES>)
    target_link_libraries(${name} PRIVATE clouds imgui glad glfw glm OpenGL::GL)
    if(CMAKE_CXX_COMPILER_ID MATCHES "GNU|Clang" AND CMAKE_BUILD_TYPE STREQUAL "Debug")
        target_compile_options(${name} PRIVATE -fsanitize=address)
        target_link_options(${name} PRIVATE -fsanitize=address)
    endif()
    add_test(NAME ${name} COMMAND ${name})
endfunction()

add_cloudlife_test(art_schedule_test art_schedule_test.cpp)
add_cloudlife_test(easelplane_test easelplane_test.cpp)
add_cloudlife_test(artfactory_test artfactory_test.cpp)

option(CLOUDLIFE_BUILD_GL_TESTS "Build integration tests requiring a window system and OpenGL" OFF)
if(CLOUDLIFE_BUILD_GL_TESTS)
    add_cloudlife_test(application_smoke_test application_smoke_test.cpp)
    add_test(NAME application_lifetime_vertex COMMAND application_smoke_test 12)
    add_test(NAME application_lifetime_vertex3d COMMAND application_smoke_test 0)
    add_test(NAME application_capture_plane COMMAND application_smoke_test 1 --capture)
    add_test(NAME application_capture_vertex COMMAND application_smoke_test 12 --capture)
    add_test(NAME application_capture_gui COMMAND application_smoke_test 1 --capture-gui)
    set_tests_properties(application_smoke_test application_lifetime_vertex
        application_lifetime_vertex3d application_capture_plane application_capture_vertex
        application_capture_gui PROPERTIES SKIP_RETURN_CODE 77)
    if(NOT APPLE)
        add_test(NAME application_capture_compute COMMAND application_smoke_test 21 --capture)
        set_tests_properties(application_capture_compute PROPERTIES SKIP_RETURN_CODE 77)
    endif()
endif()
